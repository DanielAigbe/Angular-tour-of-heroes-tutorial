// This file was previously an NgModule attempting to declare standalone components.
// The application uses standalone components and `bootstrapApplication`.
// Routing is defined in `app.routes.ts`. Keep this file as an explicit empty module
// marker to avoid accidental imports elsewhere.

export {};
